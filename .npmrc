# eslint-plugin-react@7.37.5 déclare un peer eslint "<=^9.7" alors qu'il est compatible
# ESLint 10 en pratique (les règles ne lisent que l'AST). On accepte cette résolution
# pour garder ESLint 10 + flat config sans bloquer les installs reproductibles.
legacy-peer-deps=true
